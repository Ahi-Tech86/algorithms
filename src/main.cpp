#include "../include/algorithms/searching_algorithms.h"
#include "../include/algorithms/sorting_algorithms.h"
#include "../include/algorithms/graph_algorithms.h"
#include "../include/structs/weightedGraph.h"
#include "../include/structs/graph.h"
#include <iostream>
#include <vector>

using namespace std;

int main() {
    // Graph g(5);
    // g.addEdge(0, 1);
    // g.addEdge(1, 2);
    // g.addEdge(2, 3);
    // g.addEdge(3, 4);

    // g.printGraph();

    // int startVertex = 0;
    // int goalVertex = 4;
    // auto result = dfs(g.getAdjList(), startVertex, goalVertex);
    // vector<int> path = result.first;
    // int length = result.second;

    // cout << "BFS path from vertex " << startVertex << " to vertex " << goalVertex << ":" << endl;
    // for (int i = 0; i < path.size(); i++) {
    //     if (i == path.size() - 1) {
    //         cout << path[i];
    //     } else {
    //         cout << path[i] << " -> ";
    //     }
    // }
    // cout << endl;
    // cout << "Path length: " << length << endl;
    //
    //
}
